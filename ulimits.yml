---
- name: Add ulimits for users
  hosts: pizzeria
  become: true

  tasks:
    - name: Add soft limit for open files for user
      community.general.pam_limits:
        domain: ado
        limit_type: soft
        limit_item: nofile
        value: "{{ soft_limit_value }}"

    - name: Add hard limit for open files for user
      community.general.pam_limits:
        domain: ado
        limit_type: hard
        limit_item: nofile
        value: "{{ hard_limit_value }}"

    - name: Apply limits to current systemd user session
      ansible.builtin.shell: systemctl restart user@{{ ansible_user_uid }}.service
      become: true


