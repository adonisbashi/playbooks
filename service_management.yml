---
- name: Service Management Playbook
  hosts: pizzeria
  become: true

  tasks:

    - name: Install and start HTTPD
      ansible.builtin.dnf:
        name: httpd
        state: present 
        when: "'machine2' in pizzeria"